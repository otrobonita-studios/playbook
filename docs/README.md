# Optional GitHub Pages site

This directory contains only the presentation site for `playbook.otrobonita.com`.

It is not used by either installer and is safe to delete from a derived project. The installable playbook lives in the repository root, `templates/`, and `mcp-server/`.
