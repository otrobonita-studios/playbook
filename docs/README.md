# Optional GitHub Pages site

This directory contains only the presentation site for `playbook.otrobonita.com`.

It is not used by either installer. In a derived project, this directory can be safely deleted, retained as-is, or redesigned as the project's own documentation or landing page. None of those choices affects the installable playbook in the repository root, `templates/`, and `mcp-server/`.
